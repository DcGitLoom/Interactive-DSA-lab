"""Algorithms: the procedures that operate on data.

Each module here is one family of algorithms. Everything that is worth watching
is written as a traced generator, so the same function serves the tests, the
benchmarks and the visualiser.
"""
